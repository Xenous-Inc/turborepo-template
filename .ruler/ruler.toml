# Ruler Configuration File
# See https://okigu.com/ruler for documentation.



# Default agents to run when --agents is not specified
default_agents = ["agentsmd", "claude", "cursor", "opencode"]



# --- Global MCP Server Configuration ---
[mcp]
# Enable/disable MCP propagation globally (default: true)
enabled = true
# Global merge strategy: 'merge' or 'overwrite' (default: 'merge')
merge_strategy = "merge"

# --- MCP Servers ---
# Define Model Context Protocol servers here. Two examples:
# 1. A stdio server (local executable)
# 2. A remote server (HTTP-based)

# --- MCP Server Definitions ---
[mcp_servers.context7]
command = "pnpx"
args = ["@upstash/context7-mcp"]

[mcp_servers.shadcn]
command = "pnpx"
args = ["shadcn@latest", "mcp"]

[mcp_servers.better-auth]
url = "https://mcp.better-auth.com/mcp"



# --- Global .gitignore Configuration ---
[gitignore]
# Enable/disable automatic .gitignore updates (default: true)
enabled = true
local = true  # set true to write generated ignores to .git/info/exclude instead
